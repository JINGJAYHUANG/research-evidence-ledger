# Contributing

Before opening a pull request:

```bash
python -m pip install --no-deps -e .
python tools/release_gate.py
```

Method changes must include a positive fixture and an adversarial fixture.
Never submit personal data, credentials, private decision records, proprietary
source text, or unsupported claims about real organizations.
